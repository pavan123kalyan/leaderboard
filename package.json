{
  "name": "3w-business-leaderboard",
  "version": "1.0.0",
  "description": "Dynamic Leaderboard Application with MongoDB backend and React frontend",
  "main": "index.js",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd Backend && npm install && npm start",
    "dev": "concurrently \"cd Backend && npm run dev\" \"cd frontend && npm run dev\"",
    "install-all": "npm install && cd Backend && npm install && cd ../frontend && npm install"
  },
  "keywords": ["leaderboard", "mongodb", "react", "express"],
  "author": "Your Name",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
